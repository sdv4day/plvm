int main()
{
    int a = 10;
    int b = 3;
    int sum = a + b;
    int diff = a - b;
    int product = a * b;
    int quotient = a / b;
    int remainder = a % b;
    int neg = -a;
    return sum + diff + product + quotient + remainder + neg;
}
